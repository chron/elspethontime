---
layout: layout.liquid
---
<div class="content">
  <h1>Did Elspeth make it to work on time?</h1>

  <div class="split">
    <h2>Current streak: {{ sheet | currentStreak }}</h2>
    <h2>Best streak: {{ sheet | bestStreak }}</h2>
  </div>

  <div class="calendar">
    {% for record in sheet %}
      <div class="day day--{{ record.state }}" style="--appear-delay: {{ forloop.index0 | divided_by: 12 }}s;">
        {% if record.state == 'yes' %}
          <img class="day__icon" src="/assets/trophy.svg" alt="Trophy icon" title="She made it!" />
        {% elsif record.state == 'no' %}
          <img class="day__icon" src="/assets/skull.svg" alt="Skull icon" title="She didn't make it." />
        {% endif %}
        {% if record.state != 'ghost' %}
          {{ record.date | cuteDate }}
        {% endif %}
      </div>
    {% endfor %}
  </div>
</div>

<footer>
  Made with
  <span class="heart">♥</span>
  by
  <a href="https://twitter.com/chron9">Paul</a>
</footer>
